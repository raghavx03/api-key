[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  "/opt/venv/bin/pip install --upgrade pip",
  "/opt/venv/bin/pip install -r backend/simple_requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "/opt/venv/bin/python backend/simple_main.py"
